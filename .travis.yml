language: rust

rust:
  - stable
  - beta
  - nightly
  - 1.31.0  # Rust 2018

cache: cargo

script:
  - cargo build --verbose --all-features
  - cargo test --verbose --all-features

matrix:
  allow_failures:
    - rust: nightly
